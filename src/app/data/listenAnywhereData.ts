import { ReactElement } from "react"


type AccessOptions = {
    keyIcon: ReactElement,
    keyName: string,
    firstText: string,
    buttonIcon: ReactElement,
    buttonText: string
    comments?: ReactElement
    Image: ReactElement
}

const listenAnywhereData: AccessOptions[] = [
    
]